/**
 * Problem 151: Reverse Words in a String
 *
 * Version: Follow-Up
 *
 * Constraints:
 *   1. 1 <= str.length <= 10^4
 *   2. str contains English letters (uppercase and lowercase), digits, and spaces.
 *   3. There is at least one word in str.
 */

// TODO: Finish solution.

// NOTE: JS doesn't allow in-place string manipulation. Instead, the solution should apply
//       in-place updates to the array version of the input string.

/**
 * Function: reverseWords
 * Algorithmic Paradigm: ???
 * Programming Paradigm: ???
 * Complexity:
 *   - Time: ???.
 *   - Space: ???.
 */
function reverseWords(str: string): string {
  const chars = []; // Convert `str` to array here.

  // Implementation here...

  return chars.join(' ');
}

export {};
